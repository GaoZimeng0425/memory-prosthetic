---
alwaysApply: true
---
# Memory Prosthetic - Cursor Rules

## 命名约定

| 位置 | 约定 | 示例 |
|------|------|------|
| TS 变量/函数 | camelCase | `getUserById` |
| TS 类型 | PascalCase + `type` | `type User = {}` |
| TS 文件 | kebab-case | `search-result.ts` |
| React 组件 | PascalCase | `SearchBox.tsx` |
| Rust | snake_case | `get_user_by_id` |
| API 端点 | kebab-case | `/api/collect` |
| JSON 字段 | camelCase | `{ "userId": 1 }` |

## TypeScript 规则

```typescript
// ✅ 使用 type
type User = { id: string; name: string }

// ❌ 禁止 interface
interface User { id: string }

// ✅ 使用 const 箭头函数
const handleClick = () => {}
const SearchBox = () => <div />

// ❌ 禁止 function 声明
function handleClick() {}
```

## React Compiler (已启用)

禁止手动 memoization，Compiler 自动优化：

- ❌ `useMemo`
- ❌ `useCallback`
- ❌ `memo()`

## 样式规则

1. 优先使用 `@memory-prosthetic/ui` 的 shadcn 组件
2. 使用 TailwindCSS 类名
3. 使用 `cn()` 合并类名
4. ❌ 禁止 `style={}` 属性
5. ❌ 禁止自定义 CSS 文件

## 数据获取

- ✅ 使用 `@tanstack/react-query`
- ❌ 禁止直接 `fetch()` / `axios`

## 时间处理

- ✅ 使用 `date-fns`
- ❌ 禁止 `moment` / 原生 `Date` API

## 项目结构

- 共享类型 → `packages/shared`
- UI 组件 → `packages/ui` (从 `@memory-prosthetic/ui` 导入)
- 测试文件 → 同级 `.test.ts` / `.test.tsx`

## Tauri 通信

```typescript
// Commands
const { data } = await invoke<CommandResult<T>>("command_name", { args })

// Events 命名: 领域:动作
listen("collection:completed", handler)
```

## 响应格式

```typescript
// 成功
type CommandResult<T> = { data: T }

// 错误
type CommandError = { code: string; message: string }
```

## Loading 状态

使用 `isXxxing` 格式：`isSearching`, `isCollecting`, `isLoading`
